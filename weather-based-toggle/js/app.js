/**
 * App Entry Point
 *
 * Initializes the Weather-Based Theme Toggle feature.
 */

import { initUI } from './ui-controller.js';

document.addEventListener('DOMContentLoaded', () => {
  initUI();
});
